# Mine Koçak | English Teacher

Static bilingual CV site for GitHub Pages.

- English: `/en/`
- Thai: `/th/`
- Root routing uses saved language preference first, then the browser language.
- No build step or third-party runtime dependency is required.

For local review, serve the repository with any static server, for example `python3 -m http.server 4173`.
